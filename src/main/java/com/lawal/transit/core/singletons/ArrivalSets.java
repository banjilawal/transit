package com.lawal.transit.core.singletons;

public class ArrivalSets {
    /*
    private Bag<ArrivalSet> arrivalSets;
    private static ArrivalSets singleton = null;
    private ArrivalSets () {
        this.arrivalSets = new Bag<ArrivalSet>();
    } // close constructor

    public ArrivalSets instance () {
        if (singleton == null) {
            return new ArrivalSets();
        }
        else {
            return null;
        }
    } // close instance

    public ArrivalSet search (int busRouteId, int stationId) {
        for ()
        return null;
    } // close search

     */
} // end class ArrivalSets
