package com.lawal.transit.core.singletons;
/*
import com.lawal.transit.middleware.entities.Intersection;
import com.lawal.transit.middleware.collections.Bag;
import com.lawal.transit.middleware.interfaces.BagWrapper;

import java.util.Iterator;

public enum Intersections implements BagWrapper<Intersection> {
    INSTANCE;
    Bag<Intersection> bag =  new Bag<Intersection>();

    public Bag<Intersection> getBag() {
        return bag;
    }

    public int size () { return bag.size(); }

    @Override
    public boolean add (Intersection intersection) {
        return bag.add(intersection);
    }

    @Override
    public boolean remove (Intersection intersection) {
        return bag.remove(intersection.getId());
    }

    public Iterator<Intersection> iterator () {
        return bag.iterator();
    }
    @Override
    public String toString() {
        return "Intersections" + " " + bag.toString();
    }

    public String fullString() {
        return "Intersections" + " " + bag.fullString();
    }
} // end enum Intersections

 */
